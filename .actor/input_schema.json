{
  "title": "LangGraph Agent Python",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "query": {
      "title": "Query",
      "type": "string",
      "description": "Query for the agent.",
      "editor": "textfield",
      "prefill": "Searching for a 2-bedroom apartment in San Francisco, CA, with a monthly rent between $2000 and $4000, and preferably featuring amenities such as parking and a gym.",
      "default": "Searching for a 2-bedroom apartment in San Francisco, CA, with a monthly rent between $2000 and $4000, and preferably featuring amenities such as parking and a gym."
    },
    "debug": {
      "title": "Debug",
      "type": "boolean",
      "description": "If enabled, the Actor will run in debug mode and produce more output.",
      "editor": "checkbox",
      "default": false
    }
  },
  "required": ["query"]
}
